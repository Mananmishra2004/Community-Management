<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-950 text-white flex items-center justify-center h-screen">
    <div class="w-full max-w-lg p-10 bg-zinc-900 rounded-3xl border border-blue-900/30 shadow-2xl">
        <h2 class="text-3xl font-bold mb-2">Edit Member</h2>
        <p class="text-zinc-500 text-sm mb-8 font-mono">ID: <%= user._id %></p>
        
        <form action="/update/<%= user._id %>" method="post" class="space-y-4">
            <div class="space-y-1">
                <label class="text-xs text-zinc-500">Name</label>
                <input name="name" value="<%= user.name %>" class="w-full px-5 py-3 bg-zinc-800 rounded-xl outline-none border border-transparent focus:border-blue-500">
            </div>
            <div class="space-y-1">
                <label class="text-xs text-zinc-500">Email</label>
                <input name="email" value="<%= user.email %>" class="w-full px-5 py-3 bg-zinc-800 rounded-xl outline-none border border-transparent focus:border-blue-500">
            </div>
            <div class="space-y-1">
                <label class="text-xs text-zinc-500">Profile Image URL</label>
                <input name="image" value="<%= user.image %>" class="w-full px-5 py-3 bg-zinc-800 rounded-xl outline-none border border-transparent focus:border-blue-500">
            </div>
            <div class="flex gap-4 pt-6">
                <button class="flex-1 bg-yellow-500 text-black py-3 rounded-xl font-black">Update Records</button>
                <a href="/admin" class="flex-1 bg-zinc-800 py-3 rounded-xl font-bold text-center">Back</a>
            </div>
        </form>
    </div>
</body>
</html>