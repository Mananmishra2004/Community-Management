<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-950 text-white flex items-center justify-center h-screen">
    <form action="/login" method="post" class="bg-zinc-900 border border-zinc-800 p-8 rounded-2xl shadow-2xl w-96">
        <h2 class="text-3xl font-bold mb-2 text-center">Welcome Back</h2>
        <p class="text-zinc-500 text-center mb-8 text-sm">Please enter admin credentials</p>
        
        <div class="space-y-4">
            <div>
                <label class="text-xs text-zinc-400 uppercase tracking-widest mb-1 block">Email Address</label>
                <input required class="w-full px-4 py-3 bg-zinc-800 border border-zinc-700 rounded-lg focus:border-blue-500 outline-none transition-all" type="email" name="email" placeholder="admin@gmail.com">
            </div>
            <div>
                <label class="text-xs text-zinc-400 uppercase tracking-widest mb-1 block">Password</label>
                <input required class="w-full px-4 py-3 bg-zinc-800 border border-zinc-700 rounded-lg focus:border-blue-500 outline-none transition-all" type="password" name="password" placeholder="••••••••">
            </div>
            <button type="submit" class="w-full py-3 bg-blue-600 hover:bg-blue-700 rounded-lg font-bold transition-all shadow-lg shadow-blue-600/20">Login to Dashboard</button>
        </div>
        <a href="/read" class="block text-center mt-6 text-sm text-zinc-500 hover:text-zinc-300">Continue as Guest (View Only)</a>
    </form>
</body>
</html>