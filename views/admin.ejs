<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-950 text-white flex h-screen overflow-hidden">
    <aside class="w-64 bg-zinc-900 border-r border-zinc-800 flex flex-col">
        <div class="p-6 font-bold text-2xl text-blue-500 tracking-tighter">ADMIN PANEL</div>
        <nav class="flex-1 px-4 space-y-2">
            <a href="/admin" class="block px-4 py-2 bg-blue-600 text-white rounded-lg font-medium">Dashboard</a>
            <a href="/create-page" class="block px-4 py-2 hover:bg-zinc-800 rounded-lg text-zinc-400">Add New User</a>
            <a href="/read" class="block px-4 py-2 hover:bg-zinc-800 rounded-lg text-zinc-400">Public Gallery</a>
        </nav>
        <div class="p-4 border-t border-zinc-800">
            <a href="/logout" class="text-red-500 text-sm font-medium hover:underline">Logout Session</a>
        </div>
    </aside>

    <main class="flex-1 overflow-y-auto p-10">
        <div class="flex justify-between items-center mb-10">
            <h2 class="text-3xl font-bold">User Database <span class="text-zinc-600 font-normal">(<%= count %>)</span></h2>
            <a href="/create-page" class="bg-blue-600 px-5 py-2 rounded-lg font-bold text-sm">+ Add Member</a>
        </div>

        <div class="bg-zinc-900 border border-zinc-800 rounded-2xl overflow-hidden">
            <table class="w-full text-left">
                <thead class="bg-zinc-800/50 text-zinc-500 text-xs uppercase tracking-widest">
                    <tr>
                        <th class="p-4">Profile</th>
                        <th class="p-4">Email</th>
                        <th class="p-4">Unique ID</th>
                        <th class="p-4 text-right">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-zinc-800">
                    <% users.forEach(user => { %>
                        <tr class="hover:bg-zinc-800/20 transition-all">
                            <td class="p-4 flex items-center gap-3">
                                <img src="<%= user.image %>" class="w-10 h-10 rounded-full object-cover" alt="">
                                <span class="font-semibold"><%= user.name %></span>
                            </td>
                            <td class="p-4 text-zinc-400"><%= user.email %></td>
                            <td class="p-4 font-mono text-xs text-zinc-600"><%= user._id %></td>
                            <td class="p-4 text-right space-x-4">
                                <a href="/edit/<%= user._id %>" class="text-blue-400 hover:text-blue-300">Edit</a>
                                <a href="/delete/<%= user._id %>" class="text-red-500 hover:text-red-400" onclick="return confirm('Delete this user permanently?')">Delete</a>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </main>
</body>
</html>