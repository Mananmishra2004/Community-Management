<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-950 text-white flex items-center justify-center h-screen">
    <div class="w-full max-w-lg p-10 bg-zinc-900 rounded-3xl border border-zinc-800 shadow-2xl">
        <h2 class="text-3xl font-bold mb-6">Create New Member</h2>
        <form action="/create" method="post" class="space-y-4">
            <input required name="name" class="w-full px-5 py-3 bg-zinc-800 rounded-xl outline-none" placeholder="Full Name">
            <input required name="email" class="w-full px-5 py-3 bg-zinc-800 rounded-xl outline-none" placeholder="Email Address">
            <input required name="image" class="w-full px-5 py-3 bg-zinc-800 rounded-xl outline-none" placeholder="Image URL">
            <div class="flex gap-4 pt-4">
                <button class="flex-1 bg-blue-600 py-3 rounded-xl font-bold">Save User</button>
                <a href="/admin" class="flex-1 bg-zinc-800 py-3 rounded-xl font-bold text-center">Cancel</a>
            </div>
        </form>
    </div>
</body>
</html>